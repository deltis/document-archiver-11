# Document Archiver 11

To list all tasks: `./gradlew tasks`

To build: `./gradlew build`

To clean: `./gradlew clean`

To run:   `./gradlew bootRun`

To run with specific params:   `./gradlew bootRun -DinputDir=/tmp/document-archiver/in`

Java 10

